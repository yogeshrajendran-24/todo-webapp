apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-webapp-ingress
spec:
  rules:
  - host: ""
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: todo-webapp-service
            port:
              number: 80